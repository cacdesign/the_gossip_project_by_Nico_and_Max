<h1>Liste Gossips </h1>
<p>Ceci c'est notre super site de potins, il est chouette, non ?</p>


<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">First</th>
      <th scope="col">Last</th>
      <th scope="col">Handle</th>
    </tr>
  </thead>
  <tbody>
  	<% @gossips.each_with_index do |gossip, index| %>
    <tr>
				<th scope="row"><%= index + 1 %></th>
			      <td><%= gossip.title %></td>
			      <td><%= gossip.content %></td>
			      <td><a href="/gossips/<%= index + 1 %>/show/">Voir le détail</a></td>
    </tr>
   	<% end %>
  </tbody>
</table>
